Algoritmo "circulo"
 // Área de um circulo com base no raio.
Var
raio, area :real
Inicio
  escreva("Digite o valor do raio do circulo:")
  leia(raio)
  
  area <- Pi * exp(raio,2)
  
  escreval("Area=",area:8:3)
  

Fimalgoritmo